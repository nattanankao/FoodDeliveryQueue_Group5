# กลุ่มที่ 5: Food Delivery Order Queue

## ส่วนที่ 1 การวิเคราะห์ปัญหา

### Case Study: ระบบจัดคิวสั่งซื้ออาหาร

ร้านอาหารได้รับ Order จาก Food Delivery Platform หลายรายการพร้อมกัน จึงต้องมีระบบจัดคิวเพื่อกำหนดว่า Order ใดควรถูกนำไปเตรียมอาหารก่อน โดยเปรียบเทียบ 2 วิธี คือ FIFO Queue และ Priority Queue

---

## 1. Input

ข้อมูลที่ระบบได้รับสำหรับแต่ละ Order มีดังนี้

- **Order ID** — รหัสของ Order เช่น O1, O2
- **Order Time** — เวลาหรือ ลำดับที่ Order เข้ามา
- **Food Type** — ประเภทอาหาร
- **Preparation Time** — เวลาที่ใช้เตรียมอาหาร
- **Priority** — ระดับความสำคัญของ Order

กำหนด Priority ตามโจทย์เป็น

```text
Express = Priority สูงสุด
Normal  = Priority ปานกลาง
Large   = Priority ต่ำสุด
```

ตัวอย่างข้อมูลจาก Scenario:

| Order ID | Order Time | Food Type | Preparation Time | Priority |
|---|---:|---|---:|---|
| O1 | 1 | Normal Food | 15 นาที | Normal |
| O2 | 2 | Express Food | 5 นาที | Express |
| O3 | 3 | Normal Food | 10 นาที | Normal |
| O4 | 4 | Express Food | 8 นาที | Express |
| O5 | 5 | Large Food | 25 นาที | Large |

---

## 2. Output

ระบบต้องแสดงผลเกี่ยวกับการจัดคิว ได้แก่

1. ลำดับการนำ Order ไปเตรียมอาหาร
2. Waiting Time ของแต่ละ Order
3. **Average Waiting Time**
4. **Maximum Waiting Time**
5. วิเคราะห์ **Fairness** ของแต่ละ Algorithm
6. เปรียบเทียบผลของ Algorithm A และ Algorithm B

ตัวอย่างลำดับที่ต้องการเปรียบเทียบ:

### Algorithm A: FIFO Queue

```text
O1 → O2 → O3 → O4 → O5
```

เพราะ Order ที่เข้ามาก่อนจะถูกทำก่อน

### Algorithm B: Priority Queue

```text
O2 → O4 → O1 → O3 → O5
```

เพราะ Express มี Priority สูงกว่า Normal และ Large

---

## 3. Constraints

ข้อจำกัดและเงื่อนไขของระบบมีดังนี้

- ทุก Order ต้องถูกเก็บไว้ใน Queue จนกว่าจะถูกประมวลผลหรือถูกยกเลิก
- **FIFO** ต้องรักษาลำดับการเข้ามาของ Order
- **Priority Queue** ต้องเลือก Order ที่มี Priority สูงกว่าก่อน
- ถ้า Order มี Priority เท่ากัน ให้ใช้ **Order Time** เป็นตัวตัดสิน โดย Order ที่เข้ามาก่อนจะถูกทำก่อน
- Preparation Time ของแต่ละ Order อาจไม่เท่ากัน
- Queue สามารถมี Order จำนวนมากได้
- ต้องรองรับกรณี Queue ว่าง
- ต้องรองรับกรณีมีเพียง 1 Order
- ต้องรองรับกรณีมีหลาย Order ที่ Priority เท่ากัน
- ต้องรองรับการยกเลิก Order ที่ยังอยู่ใน Queue
- ระบบต้องไม่ประมวลผล Order ที่ถูกยกเลิกแล้ว

---

## 4. Queue เก็บข้อมูลอะไร?

Queue จะเก็บข้อมูลของ **Order** แต่ละรายการ

ในแต่ละ Order มีข้อมูล:

```text
Order ID
Order Time
Food Type
Preparation Time
Priority
```

ตัวอย่าง:

```text
O2
Order Time = 2
Food Type = Express Food
Preparation Time = 5 นาที
Priority = Express
```

ดังนั้นข้อมูลหลักที่ Queue เก็บคือ **Order Object**

---

## 5. Queue ชนิดใดเหมาะสม?

งานนี้ใช้ Queue 2 ชนิดเพื่อเปรียบเทียบกัน

### Algorithm A: FIFO Queue

ใช้:

```java
Queue<Order> queue = new ArrayDeque<>();
```

เหตุผลที่เลือก `ArrayDeque`

- เหมาะกับการทำงานแบบ FIFO
- `offer()` สำหรับเพิ่ม Order เข้าท้าย Queue
- `poll()` สำหรับนำ Order ด้านหน้าออก
- `peek()` สำหรับดู Order ด้านหน้าโดยไม่เอาออก
- Enqueue และ Dequeue โดยทั่วไปใช้เวลา **O(1)**

หลักการ:

```text
เข้าก่อน → ทำก่อน
```

---

### Algorithm B: Priority Queue

ใช้:

```java
PriorityQueue<Order> queue
```

เหตุผลที่เลือก `PriorityQueue`

เพราะระบบ Food Delivery มี Order ที่ความเร่งด่วนไม่เท่ากัน

กำหนด:

```text
Express > Normal > Large
```

ดังนั้น Express Order สามารถถูกนำไปเตรียมก่อน Normal และ Large ได้

ถ้า Priority เท่ากัน จะใช้ Order Time เป็นตัวตัดสิน

---

## 6. เหตุผลที่ต้องใช้ Queue

ปัญหานี้เหมาะกับ Queue เพราะร้านอาหารอาจได้รับ Order หลายรายการพร้อมกัน และ Order เหล่านั้นต้องรอการเตรียมอาหาร

Queue ช่วยให้ระบบสามารถ:

- เก็บ Order ที่กำลังรอ
- กำหนดลำดับการประมวลผล
- เพิ่ม Order ใหม่เข้าคิว
- นำ Order ที่ถึงเวลาประมวลผลออกจากคิว
- ตรวจสอบ Order ที่อยู่หน้าคิว
- เปรียบเทียบวิธีจัดคิวหลายรูปแบบ

### เหตุผลสำหรับ FIFO

FIFO เหมาะเมื่อร้านต้องการความยุติธรรม โดยไม่ให้ Order ที่มาทีหลังแซง Order ที่มาก่อน

```text
O1 → O2 → O3 → O4 → O5
```

### เหตุผลสำหรับ Priority Queue

Priority Queue เหมาะเมื่อร้านต้องการให้ Order ที่เร่งด่วน เช่น Express ได้รับบริการก่อน

```text
Express → Normal → Large
```

---

# สรุปการวิเคราะห์ปัญหา

| หัวข้อ | คำตอบ |
|---|---|
| Input | Order ID, Order Time, Food Type, Preparation Time, Priority |
| Output | ลำดับการทำอาหาร, Waiting Time, Average Waiting Time, Maximum Waiting Time, Fairness |
| Constraints | ต้องรักษากฎ FIFO/Priority, รองรับ Queue ว่าง, 1 รายการ, หลายรายการ และ Cancel |
| Queue เก็บอะไร | Order Object |
| Algorithm A | FIFO Queue ด้วย ArrayDeque |
| Algorithm B | Priority Queue ด้วย PriorityQueue |
| เหตุผลที่ใช้ Queue | Order ต้องรอการประมวลผลและต้องกำหนดลำดับการทำงาน |

## ข้อสรุป

ระบบ Food Delivery Order Queue จำเป็นต้องใช้ Queue เพื่อจัดการ Order ที่เข้ามาหลายรายการและยังไม่สามารถเตรียมพร้อมกันทั้งหมดได้ โดย **FIFO** เน้นความยุติธรรมตามลำดับการเข้ามา ส่วน **Priority Queue** เน้นให้ Order ที่มีความเร่งด่วนสูง เช่น Express ได้รับการเตรียมก่อน
